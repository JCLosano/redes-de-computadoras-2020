ip route show

ip route add default via <<direccion>>

ip -6 route add default via <<direccion ipv6>>

ip -6 route show

tcpdump -vv

tcpdump -vv -X

arp -a

ip -s -s neigh flush all //borra tabla arp 

sudo /etc/init.d/core-daemon start

tcpdump -i any 'ip6 && icmp6 && (ip6[40] == 133 || ip6[40] == 134 || ip6[40] == 135 || ip6[40] == 136)' -v -X

dnsmasq --dhcp-range=192.168.2.13,192.168.2.254,255.255.255.0 --interface=vpeer-dhcp --no-daemon

dhclient -d




